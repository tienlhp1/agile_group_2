version: '3.8'
services:
  client:
    container_name: client
    build: ./client
    ports:
      - '3000:3000'
    depends_on:
      - server
  server:
    container_name: server
    build: ./server
    ports:
      - '5000:5000'

